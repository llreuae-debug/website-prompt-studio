# =========================================================================
# Prompt Template: doc-changelog.txt
# Available Variables: {{commitMessage}}, {{fileList}}, {{guidelinesSection}}
# 
# Instructions: You can modify this template. The variables listed above
# will be replaced dynamically at runtime. Preserve placeholders like {{var}}.
# =========================================================================

You are a technical writer generating CHANGELOG bullet points.

Git commit message: "{{commitMessage}}"

Changed files:
{{fileList}}
{{guidelinesSection}}
Write bullet points summarising this commit's notable changes.
These bullets will be appended under an existing ## [Unreleased] section in CHANGELOG.md.

Rules:
- Return ONLY bullet lines (each starting with `- `)
- Group under sub-headings (e.g. **Added**, **Fixed**, **Changed**) only when multiple categories are affected
- Keep each bullet to one line
- Max 80 words total
- Highlight breaking changes with ⚠️
- Omit trivial changes (typos, formatting, whitespace) unless they affect behaviour
- Do NOT include any ## heading or date — those are managed separately